package gov.gao.epds.test;

import java.util.Date;
import java.util.Iterator;
import java.util.Map;
import java.util.Map.Entry;

public class RemoveMapElement {

	
	public static void main(String[] args) {
		
		
		
		/*RemoveMapElementCacheBuilder.put(1234,"last", new Date());*/
		
		
		/*Iterator<Integer> iter1 = RemoveMapElementCacheBuilder.getGraphs().asMap().keySet().iterator();*/

	    /*System.out.println("Keys");
	    while (iter1.hasNext())
	        System.out.println(iter1.next());

	    System.out.println("Values");
	    Iterator<Map<String, Object>> iter2 = graphs.asMap().values().iterator();

	    while (iter2.hasNext())
	        System.out.println(iter2.next());

	    System.out.println("Entries");*/
	    /*Iterator<Entry<Integer, Map<String, Object>>> iter3 = RemoveMapElementCacheBuilder.getGraphs().asMap().entrySet().iterator();

	    while (iter3.hasNext())
	    {
	        Entry<Integer, Map<String, Object>> entry = iter3.next();
	        System.out.println(entry.getKey() + " " + entry.getValue());
	    }*/
		

	}

}
